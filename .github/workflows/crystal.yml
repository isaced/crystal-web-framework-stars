name: Crystal CI

on: push

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - name: list2md
      uses: actions/checkout@v1
      env:
        USER: isaced
        EMAIL: isaced@163.com
        REPO: crystal-web-framework-stars
        GH_REPO: github.com/isaced/crystal-web-framework-stars.git"
      run: 
        crystal list2md.cr
        ls
        git status
      
